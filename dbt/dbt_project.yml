name: 'cybersec_health'
version: '1.0.0'
config-version: 2

profile: 'cybersec_health'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  cybersec_health:
    staging:
      +materialized: view
    marts:
      +materialized: table